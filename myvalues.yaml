# Override configuration from https://github.com/jenkins-x/jenkins-x-platform/blob/master/values.yaml
exposecontroller:
  exposecontroller:
    HTTP: "true"
    TLSACME: "false"

chartmuseum:
  enabled: false

monocular:
  enabled: false

nexus:
  enabled: false

docker-registry:
  enabled: false

gitea:
  enabled: false

jenkins:
  enabled: true
